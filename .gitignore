/target
ff-kernel/target
Cargo.lock
ff-kernel/Cargo.lock
/output.png

# Artefacts copied to the repo root during manual deploy/debug.
/et-radiosity
/ff-kernel.elf

# Bug reports (shared with et-rs team separately; not part of the commit history).
/et-bug.md
