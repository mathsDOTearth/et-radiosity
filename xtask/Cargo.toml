[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
description = "Build, deploy, and run tasks for et-radiosity (cargo xtask)"

[dependencies]
anyhow = "1"
clap   = { version = "4", features = ["derive"] }
